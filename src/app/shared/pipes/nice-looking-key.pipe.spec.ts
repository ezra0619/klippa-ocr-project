import { NiceLookingKeyPipe } from './nice-looking-key.pipe';

describe('NiceLookingKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new NiceLookingKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
